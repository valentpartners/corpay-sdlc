# Paths mirrored into each story worktree on creation by
# scripts/run-codex-loop.sh.
#
# Use for files the agent needs locally but that should never be pushed to
# GitHub (typically also listed in .gitignore).
#
# Cleanup copies story run artifacts back from worktrees before teardown.
# One path per line. Trailing slash for directories. Comments start with #.

AGENTS.md
.worktreeinclude
.codex/aisdlc.json
.codex/settings.json
CONTEXT.md
.codex/README.md
.codex/agents/
.codex/knowledge/
.codex/rules/
.codex/skills/
docs/
scripts/
